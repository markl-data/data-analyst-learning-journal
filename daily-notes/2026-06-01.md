# 2026-06-01 - Day 3: Day 2 Finished + Consolidation

## What I did Today
- Morning: completed Day 2 Sets C–E (15 exercises)
- Afternoon: active recall block (R1–R10) + combined pattern block (P1–P7)

## Active recall - what came back without reference
- GROUP BY + HAVING when counting or summing

- Filtering with WHERE  
- Consistently remembered how to filter paid/unpaid, extract months, etc.

- Aggregate functions  
- SUM, AVG, MIN, MAX, COUNT.

- Ordering results  
- Always remembered to sort when the question required it.

- Extracting parts of dates  
- Used EXTRACT(YEAR/MONTH) correctly once you saw the pattern.

## Active recall - what was fuzzy or needed reference
- Grouping by the wrong column  
- (e.g., grouping by product_id instead of customer_id)

- Using aliases inside HAVING  
- SQL doesn’t allow that - you must repeat the aggregate.

- Trying to filter multiple values incorrectly  
- I wrote:
- WHERE status = 'paid', 'unpaid'  
- instead of
- WHERE status IN ('paid','unpaid')

- Using COUNT instead of SUM  
- Sometimes counted rows when you meant to add quantities.

- Grouping by full dates instead of year/month  
- Grouped by invoice_date instead of EXTRACT(YEAR).

- Selecting non‑grouped columns  
- (e.g., selecting product_id while grouping by category)

## Combined patterns - what felt natural vs awkward
- Natural
- GROUP BY + HAVING  

- Filtering with WHERE  
- Comfortable filtering rows before grouping.

- Aggregates  
- You know when to use SUM/AVG/MIN/MAX.

- Akward

- Understanding when NOT to use GROUP BY  
- For “across all data” questions, accidentally grouped by invoice_id.

- Using HAVING correctly  
- You sometimes put the wrong expression inside HAVING.

- Understanding JOIN‑dependent questions  
- Not doing JOINs yet, so I get stuck on questions that require them.

## Open questions for tomorrow
- Why can’t I use an alias in HAVING?  
- Tried HAVING total_amount > 10000.

- Why must GROUP BY match the non‑aggregated columns?  
- Selected product_id while grouping by category.

- How do I know which table to use?  
- Tried to get quantities sold from the products table.

- When do I use SUM vs COUNT?  
- Sometimes counted rows instead of summing quantities.

- How do I group by month/year properly?  
- Grouped by invoice_date instead of EXTRACT(MONTH).

## Tomorrow's plan
- Day 4: JOINs introduction (the big one)